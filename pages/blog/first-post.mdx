import Counter from "../../components/Counter"
import Post from "../../components/Post"

export const metadata = {
	"title": "First post", 
 	"description": "This a description for the first post that was written by me", 
 	"date": "2020-01-01T18:00:00Z"
}

export default ({ children }) => <Post meta={metadata}>{children}</Post>

# This is the post with the react component 


<Counter>Click me!</Counter>
